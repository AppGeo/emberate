//var generator = require('../../loom/generators/model');

//describe('model', function() {
  //describe('present', function() {
    //var env = {
      //args: ['user'],
      //params: { name: 'string', age: 'number' },
      //name: 'model'
    //};
    //var expected = {
      //objectName: 'User',
      //params: undefined,
      //fields: [
        //{ name: 'name', type: 'string', comma: ',' },
        //{ name: 'age', type: 'number', comma: '' }
      //]
    //};

    //it('adds fields to locals', function() {
      //generator.present(env.args[0], env.params, env).should.eql(expected);
    //});

    //it('skips any args inbetween the name and the params', function() {
      //generator.present(env.args[0], 'fluffy', 'fluff', env.params, env).should.eql(expected);
    //});
  //});
//});

